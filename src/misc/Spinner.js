import React from "react";
import "./Spinner.css";

/** oLoading message used by components that fetch API data */

function Spinner() {
    return (
        <div className="Spinner">
            Loading . . .
        </div>
    );
}

export default Spinner;